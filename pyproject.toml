[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "resolution_week3_auroraaa"
version = "0.0.2"
dependencies = []

[project.scripts]
todo = "resolution_week3_auroraaa.main:main"